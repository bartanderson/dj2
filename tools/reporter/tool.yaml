name: findings_reporter
description: Formats and displays findings
execution: cli
path: tools/reporter/run.py
provides:
  - action: report.findings
  input_format: json
    description: Generate human-readable report
    inputs:
      - name: data
        type: object
      - name: format
        type: string
    outputs:
      - name: report
        type: string